namespace SIC.Web.Services.PrePedidosPDF;

/// <summary>
/// Cliente HTTP tipado para a API de Pré-Pedidos.
/// Dividido em partial classes: Queries (leitura) e Commands (escrita).
/// </summary>
public sealed partial class PrePedidoPDFApiClient(HttpClient httpClient)
{
}
