@page
@model SIC.Web.Pages.Account.ForgotPasswordModel
@{
    ViewData["Title"] = "Esqueci minha senha";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm">
            <div class="card-header">
                <h5 class="mb-0">Esqueci minha senha</h5>
            </div>
            <div class="card-body">
                @if (!string.IsNullOrWhiteSpace(Model.Message))
                {
                    <div class="alert @(Model.Success ? "alert-success" : "alert-danger")">@Model.Message</div>
                }

                <form method="post">
                    <div class="mb-3">
                        <label asp-for="Identifier" class="form-label">Login ou e-mail</label>
                        <input asp-for="Identifier" class="form-control" />
                        <span asp-validation-for="Identifier" class="text-danger"></span>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a asp-page="/Account/Login">Voltar ao login</a>
                        <button type="submit" class="btn btn-primary">Solicitar redefinição</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
